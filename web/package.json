{
  "name": "cinegeist-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "The static browser demo for CineGeist: the deterministic recommender, no LLM, no server.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "~5.7.2",
    "vite": "^6.0.7",
    "vitest": "^3.0.2"
  }
}

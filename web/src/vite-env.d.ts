/// <reference types="vite/client" />

// Injected by Vite's `define` (vite.config.ts): a short content hash of the committed shard, used
// to cache-bust the shard fetch so a redeploy can't serve stale bytes.
declare const __SHARD_HASH__: string;

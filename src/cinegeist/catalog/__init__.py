"""The catalog: SQLite metadata plus the numpy tag-genome memmap.

Session 2 fills this package out — schema and migrations here in ``db.py``, the MovieLens
and TMDB ingest under ``sources/``, the genome memmap in ``genome.py``, and the pipeline
orchestrator in ``build.py``. See ``plan.md`` §2 for what the catalog is built from.
"""
